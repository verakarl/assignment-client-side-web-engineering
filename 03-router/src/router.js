/**
 * Implement a depenerouter for web browsers
 *
 * API:
 *
 * - page(path, function)
 * - page('/', index)
 * - page('/user/:user', show)
 * - page('/user/:user/edit', edit)
 * - page('/user/:user/album', album)
 * - page('/user/:user/album/sort', sort)
 * - page('*', notfound)
 * - page()
 */

export default function router(path, fn) {

}
